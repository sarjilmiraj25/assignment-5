Answer-1:
var--- it can be redeclared and updated.
let---- it cannot be redeclared in the same scope, but its value can be changed.
const---- cannot be redeclared or updated. It must be given a value when declared.

Answer-2: The spread operator in js is used to spread the elements of an array.

Answer-3:
map() ----- creates a new array by transforming every single element of an array.

filter()----- creates a new array with those elements which are passed by a condition.

filter()---- loops through an array but doesn't return a new array.

Answer-4: Arrow function is the shorter way to write a function using the => syntax.Most commonly it has been used in ES6 to make functions more easier to write.

Answer-5: Template literals ara a feature in js to create string. We use this sign(``)  to write like string.In this method we can also use variables value in a string.We can also break a line anytime and anywhere if we use this. 